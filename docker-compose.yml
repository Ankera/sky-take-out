services:
  sky-frontend:
    image: nginx:stable-alpine3.23-perl
    container_name: sky-frontend
    ports:
      - "7788:80"
    volumes:
      - ./html/sky:/usr/share/nginx/html:ro
      - ./html/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
